import React, { Component } from "react";
import { View, Text } from "react-native";

const ServiceBox = ({ type, icon, service }) => {
  return (
    <View>
      <Text> textInComponent </Text>
    </View>
  );
};

export default componentName;
